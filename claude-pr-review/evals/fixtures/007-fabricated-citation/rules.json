{
  "CLAUDE.md": "A finding blocks merge only if it cites a requirement, a specific named failing test, or a documented convention. Anything else is a nit: recorded, never blocking.\n\n- Tests are written after implementation, from the spec - not TDD.\n- Security-sensitive behaviour (auth, credential storage, credential exposure) needs a test that asserts it directly, not incidentally through a success path.",
  "openspec/config.yaml": "",
  "sdd_django_demo/CLAUDE.md": ""
}
