[pytest]
# Scoped to harness/ on purpose: sdd_django_demo/ has its own pytest.ini and
# needs DJANGO_SETTINGS_MODULE, so it is run from its own directory.
testpaths = harness
python_files = test_*.py
