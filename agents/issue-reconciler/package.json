{
  "name": "issue-reconciler",
  "private": true,
  "type": "module",
  "description": "Sweeps the project board, reconciles issue status against actual repo state (PRs, OpenSpec proposals), and converges the two without duplicating work or overriding a human.",
  "scripts": {
    "test": "node --test --experimental-strip-types"
  },
  "engines": {
    "node": ">=23"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.125.0",
    "zod": "^4.6.5"
  }
}
