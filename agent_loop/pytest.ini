[pytest]
# Run from the repository root, so that `import agent_loop` resolves without installation:
#   agent_loop/.venv/bin/python -m pytest agent_loop/tests
testpaths = tests
python_files = test_*.py
